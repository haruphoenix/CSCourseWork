#ifndef ARCSHADER_H
#define ARCSHADER_H
#include <GL/glew.h>

class ArcShader
{
 public:
  ArcShader(const char* filename, GLenum shaderType);
 private:
  GLuint mHandle;
  GLuint loadShader();
};

#endif
